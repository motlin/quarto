# Everything under /assets/ carries a content hash in its name (the solver wasm,
# the opening books, the JS chunks), so browsers can keep it for a year.
/assets/*
  Cache-Control: public, max-age=31536000, immutable
